<li>
  <%= gravatar_for user, size: Settings.user.gravatar.for_user %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.delete"), user, method: :delete,
      data: {confirm: t("users.you_sure")} %>
  <% end %>
</li>
